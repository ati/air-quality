<!DOCTYPE html>
<html>
    <head>
        <title>Московский воздух: <%= yield_content(:title) %></title>
        <link href="/bootstrap/css/bootstrap.css" rel="stylesheet" />
        <link href="/bootstrap/css/bootstrap-responsive.css" rel="stylesheet" />
        <%= yield_content :stylesheets %>
        <script type="text/javascript" src="/js/jquery.min.js"></script>
        <%= yield_content :scripts %>

		<script type="text/javascript">

		  var _gaq = _gaq || [];
		  _gaq.push(['_setAccount', 'UA-34430453-1']);
		  _gaq.push(['_trackPageview']);

		  (function() {
			var ga = document.createElement('script'); ga.type = 'text/javascript'; ga.async = true;
			ga.src = ('https:' == document.location.protocol ? 'https://ssl' : 'http://www') + '.google-analytics.com/ga.js';
			var s = document.getElementsByTagName('script')[0]; s.parentNode.insertBefore(ga, s);
		  })();

		</script>
    </head>
    <body>
      <div class="container">
        <header>
          <%
            menu_items = {
              ''            => 'О воздухе',
              'methodology' => 'Что и как измеряется',
              'contacts'    => 'Контакты',
            }
          %>
          <div class="navbar">
            <div class="navbar-inner">
              <a class="brand" href="/"><i class="icon-th"></i> Московский воздух</a>
              <ul class="nav">
                <li class="divider-vertical"></li>
                <% menu_items.keys.each do |mik| %>
                  <li<% if request.path_info == '/' + mik %> class="active"<% end %>><a href="/<%= mik %>"><%= menu_items[mik] %></a></li>
                <% end %>
              </ul>
            </div>
          </div>
          </header>
  
        <%= yield %>
        <!-- footer -->
        <!--
        <footer>
          <div class="row" style="position:fixed; bottom:0;">
            http://vozduh.msk.ru, 2012.
          <div>
        </footer>
        -->
      </div>
    </body>
</html>
